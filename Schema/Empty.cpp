// Anchor translation unit for SchemaLib. Including the generated SBE headers here means the
// library build itself compiles them, so every static_assert(sizeof == BlockLength) is
// validated on every build. Mirrors HFT/*/Empty.cpp.
#include "ILink3Sbe.hpp"
#include "Mdp3Sbe.hpp"
