// Gives the header-only MDP3 library a translation unit, so the build generates compile
// commands for it and runs any static assertions in its headers.

#include "ChannelConfig.hpp"
#include "UdpReceiver.hpp"
