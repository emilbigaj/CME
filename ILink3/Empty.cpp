// Anchor translation unit for ILink3Lib. Including the generated SBE header here means the
// library build itself compiles it, so every `static_assert(sizeof == blockLength)` is
// validated on every build -- not only when the test app pulls it in. Mirrors HFT/*/Empty.cpp.
#include "Generated/ILink3Sbe.hpp"
